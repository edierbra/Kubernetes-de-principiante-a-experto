apiVersion: v1
kind: Pod
metadata:
  name: limit-cpu
spec:
  containers:
  - name: cpu-demo
    image: vish/stress
    resources:
      requests:
        cpu: "15"
      limits:
        cpu: "15"
    args:
    - -cpus
    - "2"